export default function Card (props){
    console.log(props);
    return <div>
        <h1>Card Title {props.name}</h1>
                <h1>Card Title {props.name}</h1>
                                <h1>Card Title {props.name}</h1>


    </div>
}